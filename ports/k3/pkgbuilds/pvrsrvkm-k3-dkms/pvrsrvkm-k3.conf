# The K3's GPU node lists both drivers' compatibles. With SpacemiT's DDK driver
# installed, it drives the GPU (OpenGL ES 3.2 through SpacemiT's libraries);
# the mainline powervr driver, which only has a Vulkan user space so far, stays
# out of the way.
blacklist powervr
